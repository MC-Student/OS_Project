public class slaveBFromMaster extends Thread
{
    /*
     * Takes in:
     *   1. toCompleteB job list
     *   2. slaveB socket input stream from master
     *   3. lock on the arraylist
     *
     * wait for job on the input stream
     * when job added
     *   synch on the arraylist
     *       add job
     * */
}
